/**
 * The self-DSAR export (Art. 15(1)(h)): everything the store holds, with the
 * logic stated — the rows AND the reasons, not just the rows.
 */
import { selfDsar } from '../egress';

console.log(selfDsar());
